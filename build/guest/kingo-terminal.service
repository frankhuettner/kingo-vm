[Unit]
Description=Kingo web terminal (ttyd) with OpenCode on http://localhost:7681
After=network.target

[Service]
User=student
WorkingDirectory=/home/student
ExecStart=/usr/bin/ttyd --writable --port 7681 bash -l
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
